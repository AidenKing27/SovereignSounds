@page
@model SovereignSounds.Pages.Music.Genres.ViewModel

@{
    ViewData["Title"] = "Genres";
}

<div class="row">
    <div class="col-12">
        <div class="card shadow-sm">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center flex-wrap p-2 gap-2">
                    <h1 class="mb-0">Genres</h1>
                    <a asp-page="Create" class="btn btn-primary">
                        <span class="bi bi-plus-lg btn-icon"></span>
                        Create New
                    </a>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle music-table">

                        <colgroup>
                            <col style="width: 75%" />  @* Genre *@
                            <col style="width: 25%" />  @* Actions *@
                        </colgroup>

                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Genres[0].Name)
                                </th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Genres)
                            {
                                <tr>
                                    <td class="music-primary-text">
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td class="text-end">
                                        <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary me-1">
                                            <span class="bi bi-pencil-square btn-icon"></span>
                                        </a>
                                        <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                            <span class="bi bi-trash"></span>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
