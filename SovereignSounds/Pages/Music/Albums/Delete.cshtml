@page
@model SovereignSounds.Pages.Music.Albums.DeleteModel

@{
    ViewData["Title"] = "Delete Album";
}

<div class="row justify-content-center">
    <div class="col-12 col-lg-10">
        <div class="card shadow-sm">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center flex-wrap p-2 gap-2">
                    <h1 class="mb-0">Delete Album</h1>
                    <a asp-page="./View" class="btn btn-secondary">
                        <span class="bi bi-arrow-return-left me-2"></span>
                        Back to List
                    </a>
                </div>
            </div>
            <div class="card-body p-4">
                <p class="alert alert-danger mb-4">Are you sure you want to permanently delete this album?</p>

                <dl class="row mb-0">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Album.Title)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Album.Title)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Album.Artist)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Album.Artist)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Album.ReleaseDate)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Album.ReleaseDate)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Album.Price)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Album.Price)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Album.Picture)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Album.Picture)</dd>
                </dl>
            </div>
            <div class="card-footer">
                <form method="post" class="d-flex justify-content-end">
                    <input type="hidden" asp-for="Album.Id" />
                    <button type="submit" class="btn btn-danger">
                        <span class="bi bi-trash me-2"></span>
                        Delete Album
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
